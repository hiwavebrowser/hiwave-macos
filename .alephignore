# Trench night-2 (Atlas, 2026-07-08): port of Athena's vendor-mask approach
# (hiwave-windows@b692647). Entries are directory NAMES matched at any depth
# (aleph 0.5.0). vendor/ and target/ are already default-masked; the macOS
# equivalent of her deps/ finding is the capture/baseline churn below —
# binary trees that pollute temporal git-log scans and symbol discovery.
# Session-7: fastrender is NOT the metric engine (parity renders with
# crates/rustkit-*) — keep it masked so Aleph steers to rustkit.
fastrender

.browsers
parity-baseline
parity-history
multisurface-captures
builtins-captures
builtins-baselines
builtins-diffs
baselines
goldens
websuite
