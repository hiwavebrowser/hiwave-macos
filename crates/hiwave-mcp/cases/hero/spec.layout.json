{
  "case": "hero",
  "stage": "layout",
  "kind": "expectations",
  "origin": "hand-derived from the CSS in page.html — CSS 2.1 box model, default content-box sizing",
  "viewport": { "width": 800, "height": 600 },
  "expect": [
    { "path": "root.border_box.width",
      "value": 800.0,
      "why": "the initial containing block is the viewport" },
    { "path": "root.border_box.height",
      "value": 600.0,
      "why": "the initial containing block is the viewport" },

    { "path": "root.children[0].children[0].content_rect.width",
      "value": 400.0,
      "why": ".hero declares width: 400px; content-box sizing means that IS the content width" },
    { "path": "root.children[0].children[0].content_rect.height",
      "value": 120.0,
      "why": ".hero declares height: 120px" },
    { "path": "root.children[0].children[0].border_box.width",
      "value": 432.0,
      "why": "400 content + 16 padding-left + 16 padding-right, no border declared" },
    { "path": "root.children[0].children[0].border_box.height",
      "value": 152.0,
      "why": "120 content + 16 padding-top + 16 padding-bottom, no border declared" },
    { "path": "root.children[0].children[0].border_box.x",
      "value": 0.0,
      "why": "body margin is 0, so the hero starts at the viewport origin" },
    { "path": "root.children[0].children[0].border_box.y",
      "value": 0.0,
      "why": "body margin is 0, so the hero starts at the viewport origin" },
    { "path": "root.children[0].children[0].padding.left",
      "value": 16.0,
      "why": "`padding: 16px` expands to all four sides" },
    { "path": "root.children[0].children[0].padding.top",
      "value": 16.0,
      "why": "`padding: 16px` expands to all four sides" },

    { "path": "root.children[0].children[0].children[0].border_box.width",
      "value": 400.0,
      "why": "h1 is a block with auto width, so it fills the hero's 400px content box" },
    { "path": "root.children[0].children[0].children[0].border_box.x",
      "value": 16.0,
      "why": "the hero's content box starts one padding-left in from its border box at x=0" }
  ]
}
